// 2. Function with Parameters: Create a function doubleNumber that takes a number as an argument and returns the number multiplied by 2.

function doubleNumber(number) {
    return number * 2;
  }
  
  console.log(doubleNumber(5));
  